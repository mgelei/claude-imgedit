Use pip, not uv, when installing Python packages in this repo.
Always use .venv when interacting with Python.
Github CLI is available, use it to interact with the repo and issues.
Do not create bogus unit tests, since they can't validate the model's behavior locally.
